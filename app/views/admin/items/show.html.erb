<div class="container">
  <div class="row">
    <h4 class="header-background col-xs-8 col-xs-offset-2">商品詳細</h4>
  </div>

  <div classs="row">
    <div class="col-xs-2 col-xs-offset-1">
      <%= attachment_image_tag(@item, :image_id, size: '170x130' ) %>
    </div>

    <div class="col-xs-7 col-xs-offset-1">
      <table>
        <tr class="row">
          <th class="col-md-4">商品名</th>
          <td class="col-md-8"><%= @item.name %></td>
        </tr>
        <tr class="row table-lign">
          <th class="col-md-4">商品説明</th>
          <td class="col-md-8"><%= @item.specifcation %></td>
        </tr>
        <tr class="row">
          <th class="col-md-4">ジャンル</th>
          <td class="col-md-8"><%= @genre.genre_name %></td>
        </tr>
        <tr class="row">
          <th class="col-md-4">税込価格(税抜価格)</th>
          <td class="col-md-8">
            <%= @item.plus_tax %>
            (<%= @item.price_without %>)円
          </td>
          </th>
        </tr>
        <tr class="row">
          <th class="col-md-4">販売ステータス</th>
          <td class="col-md-8">
            <% if @item.item_status == true %>
              <%= "販売中" %>
            <% else %>
              <%= "販売中止" %>
            <% end %>
          </td>
        </tr>
      </table>
      <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-info pull-right btn-lg" %>

    </div>
  </div>
</div>