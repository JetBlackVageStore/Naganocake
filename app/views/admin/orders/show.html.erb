      <h2>　<span class="bg-secondary text-white">注文履歴詳細<br></span>　</h2>
      <table class="table">
        <tr>
          <th>購入者</th>
          <th>配送先</th>
          <th>支払方法</th>
          <th>注文ステータス</th>
        </tr>

        <tr>

          <td>
            <%= link_to admin_customer_path do %>
              <%= @order.customer.surname %>　<%= @order.customer.firstname %>
            <% end %>
          </td>

          <td>〒：<%= @order.postal_code %>
              住所：<%= @order.address %>
            　名前：<%= @order.addressee %>
          </td>

          <td><%= @order.payjp_id %></td>
          <td>
            注文ステータス：
            <%= form_with model: @order, url: admin_order_path, method: :patch, local: true do |f| %>
              <%= f.select :order_status,Order.order_statuses.keys.to_a %>
              <%= f.submit "更新" %>
            <% end %></td>
<!--          <td>-->
            <%#= form_with model: @order, url: admin_order_path, method: :patch, local: true do |f| %>
              <%#= f.select :order_status,Order.order_statuses.keys.to_a %>
              <%#= f.submit "更新" %>
            <%# end %>
<!--          </td>-->
        </tr>
      </table>

      <table class="table">
        <thead>
        <tr>
          <th>ID</th>
          <th>商品名</th>
          <th>単価（税込）</th>
          <th>数量</th>
          <th>小計</th>
          <th colspan="5">製作ステータス</th>
        </tr>
        </thead>
        <tbody>
        <% @order_items.each do |oi| %>
          <tr>
            <td>id:<%= oi.id %></td>

            <td><%= oi.item.name %></td>

            <td><%= ((oi.item.price_without).to_i * 1.1).round %></td>

            <td><%= oi.quantity %></td>

            <td><%= @syoukei = ((oi.item.price_without).to_i * 1.1 * (oi.quantity).to_i).round %></td>


            <td>
              <%= form_with model: oi, url: admin_orders_item_path(oi), method: :patch, local: true do |f| %>
                <%= f.select :making_status, OrderItem.prod_statuses.keys %>
                <%= f.submit "更新", class: "btn btn-sm btn-primary" %>
              <% end %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
    <div class="col-4 align-self-center">
      <table class="table table-borderless">
        <tbody>
        <tr>
          <td>商品合計：</td>
          <td><%= @order.payment  %>円</td>
        </tr>
        <tr>
          <td>送料：</td>
          <td>800円</td>
        </tr>
        <tr>
          <td>請求額合計：</td>
          <td><%=@order.payment.to_i + 800 %>円</td>
        </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>